[tool:pytest]
markers =
    smoke: Smoke tests
    regression: Regression tests
    slow: Slow running tests
addopts =
    --html=reports/report.html
    --self-contained-html
    -v
testpaths = tests